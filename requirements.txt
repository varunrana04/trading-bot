# Core dependencies
# Python 3.8+ required (not listed in requirements.txt)

# Data processing (REQUIRED)
pandas>=2.0.0
numpy>=1.24.0

# Live market data (REQUIRED for live trading)
yfinance>=0.2.18
python-binance>=1.0.17

# Scientific computing (REQUIRED)
scipy>=1.10.0
scikit-learn>=1.3.0

# Technical analysis (OPTIONAL - can work without it)
# TA-Lib>=0.4.28  # Commented out - difficult to install on Windows

# Configuration (REQUIRED)
pyyaml>=6.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Date/time handling (REQUIRED)
python-dateutil>=2.8.0
pytz>=2023.3

# Options pricing (REQUIRED for Greeks calculation)
py_vollib>=1.0.1

# Web framework (REQUIRED for dashboards)
flask>=2.3.0
flask-cors>=4.0.0
flask-socketio>=5.3.0
python-socketio>=5.10.0

# ============================================
# OPTIONAL DEPENDENCIES (for advanced features)
# ============================================

# API and Web (for REST API and dashboard)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0
aiohttp>=3.9.0
python-multipart>=0.0.6

# Database (for persistent storage)
sqlalchemy>=2.0.0
alembic>=1.12.0
psycopg2-binary>=2.9.0
asyncpg>=0.28.0

# Caching (for performance)
redis>=5.0.0
hiredis>=2.2.0

# Message queue (for distributed systems)
pika>=1.3.0

# Logging and monitoring
structlog>=23.2.0

# Testing (for development)
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# Charting and visualization
plotly>=5.18.0
matplotlib>=3.8.0

# PDF generation (for reports)
reportlab>=4.0.0

# Development tools
black>=23.0.0
isort>=5.12.0
flake8>=6.0.0
mypy>=1.5.0

# Utilities
click>=8.1.0
rich>=13.0.0
tqdm>=4.65.0
